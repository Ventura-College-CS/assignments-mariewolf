#include "final1.hpp"
#include <fstream>

const int N = 10;

void makeArray(Course []);

int main() {
    Course arr[N];
    makeArray(arr);

    return 0;
}

void makeArray(Course arr) {
    for (int i=0; i<N; i++) {
        //read file
    }

    //sort array
}